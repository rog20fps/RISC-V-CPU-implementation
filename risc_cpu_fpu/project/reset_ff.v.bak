module reset_ff #(parameter V = 32) (
    input       clk, rst,
    input       [V-1:0] d,
    output reg  [V-1:0] q
);

always @(posedge clk or posedge rst)
begin
    if (rst) q <= 0;
    else     q <= d;
end

endmodule
